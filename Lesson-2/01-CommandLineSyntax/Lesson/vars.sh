#! /bin/bash
# $HOME - Current users home directory
# $PS1 - main
# $PATH - colon separated list os directories where the shell can look for commands.


echo "This is the current users home directory: $HOME"
echo "This is where your shell is looking for binaries or commands\n: $PATH"
